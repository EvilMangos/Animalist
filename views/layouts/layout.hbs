{{> head}}

<body>
    <div class="body__inner">
        {{> header}}
        <section class="main">
            {{> categories}}
            <div class="part main__inner">
                {{> menu}}
                <div class="content">
                    {{{body}}}
                </div>
        </section>
        {{> modalWindows}}
    </div>
    {{> footer}}
    {{> scripts}}
</body>

</html>